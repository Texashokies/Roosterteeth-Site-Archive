{% set relPath = ".." if page.outputPath.split('/').length == 3 else "." %}
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="{{ relPath }}/lib/tailwind.css" rel="stylesheet">
    <link rel="stylesheet" href="{{ relPath }}/dist/light.css" />
    <script src="{{ relPath }}/dist/webreplaygen.js"></script>
    <script>
        function clickTab(tabName){
            console.log("clicking tab: ", tabName);
            if(tabName === "all"){
                let x = document.getElementsByClassName('archive');
                console.log("Found archives: ", x);
                for(let i =0;i<x.length;i++){
                    x[i].style.display = "block";
                }
            } else{
                let y = document.getElementsByClassName('archive');
                console.log("Found archives: ", y);
                for(let i =0;i<y.length;i++){
                    y[i].style.display = "none";
                }
                let x = document.getElementsByClassName(tabName);
                console.log("Found tab relevant archives: ", x);
                for(let i =0;i<x.length;i++){
                    x[i].style.display = "block";
                }
            }
        }
    </script>
  </head>
  <body>
    {% include "header.njk" %}
    {{ content | safe }}
  </body>
</html>
